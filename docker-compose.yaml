services:
  zeebe:
    image: camunda/zeebe:8.5.0
    container_name: zeebe-local
    ports:
      - "26500:26500" # GRPC port
      - "8080:8080"   # REST API and Zeebe Management
    environment:
      - ZEEBE_STANDALONE=true